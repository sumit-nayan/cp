#include<bits/stdc++.h>
#define int long long
using namespace std;
int n;
vector<int> a;
signed main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    int n;
    cin>>n;
    a = vector<int> (n);
    for(int i=0;i<n;i++){
        cin>>a[i];
    }

    


    return 0;
}